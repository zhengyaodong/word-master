# 微信小程序背单词项目需求文档（PRD）V3.0
## 版本信息
- **版本号**: V3.0
- **更新日期**: 2026-02-07
- **更新说明**: 在 V2.0 基础上新增“批量导入与一键洗词、AI 语境助记”等能力，重点解决批量录入与记忆效率问题。

---

## 1. 项目概述

### 1.1 背景
现有功能已覆盖查词、生词本、语音朗读、收藏与学习统计，但录入成本高、记忆方式单一。V3.0 目标是降低录入门槛并增强记忆方式。

### 1.2 V3.0目标
- 提供批量导入与“一键洗词”能力，降低录入成本
- 在单词详情页提供“AI 助记”，增强记忆效率

### 1.3 技术栈
- **前端**: 微信小程序 + TypeScript + Less
- **后端**: Python + Flask
- **数据库**: SQLite3
- **AI**: 本地 Ollama 模型
- **TTS**: Edge-TTS

---

## 2. V3.0 功能模块

### 2.1 批量导入与“一键洗词”(Data Processing)

#### 功能描述
支持直接粘贴长文本（文章、歌词、PDF 复制内容），通过本地模型或规则提取“核心词汇/生词”，并一键加入生词本。

#### 功能要点
- 支持输入大段文本（文本框/粘贴）
- 一键“洗词”：输出候选词列表
- 过滤高频词（如 the, and, of 等）与非字母内容
- 结果预览与去重提示
- 选中后批量加入生词本

#### AI 介入
- 调用本地 Qwen 模型提取核心词汇
- 简化 Prompt：从文本中提取英文词汇，剔除常见词并给出去重列表

#### 可行性评估
- 可实现。文本处理 + 轻量模型推理，0.6B 规模足够。
- 风险点：长文本 prompt 超限、提取稳定性。
- 规避：分段处理 + 词表去重；必要时加入规则兜底（正则分词 + 高频词表）。

---

### 2.2 AI 语境辅助记忆 (Context Mnemonics)

#### 功能描述
单词详情页新增“AI 助记”按钮，生成极短幽默故事或谐音联想，提升记忆强度。

#### 功能要点
- 每个词生成 1-2 句极短助记
- 支持一键刷新（可选）
- 结果缓存到数据库，避免重复生成

#### AI 介入
- Prompt 引导生成中文谐音/联想故事
- 示例：Ambition -> “俺必胜（Am-bi-tion），因为我有野心。”

#### 可行性评估
- 可实现。生成内容短、成本低，0.6B 模型足够。
- 风险点：生成质量波动。
- 规避：Prompt 模板 + 兜底（如果输出为空或过长，重新生成或给默认提示）。

---

## 3. 数据库设计

### 3.1 新增字段（vocabulary_book）
| 字段 | 类型 | 说明 |
|------|------|------|
| mnemonic | TEXT | AI 助记内容（缓存） |
| mnemonic_updated_at | TIMESTAMP | 助记更新时间 |

### 3.2 新增表：import_history（导入记录）
| 字段 | 类型 | 说明 |
|------|------|------|
| import_id | INTEGER | 主键 |
| user_id | INTEGER | 用户ID |
| source_type | VARCHAR(50) | 来源（paste/csv/json） |
| raw_text | TEXT | 原始文本 |
| word_count | INTEGER | 提取词数 |
| created_at | TIMESTAMP | 创建时间 |

---

## 4. API 设计

### 4.1 洗词/导入
| 接口 | 方法 | 功能 |
|------|------|------|
| /api/vocab-book/clean | POST | 一键洗词（文本->词列表） |
| /api/vocab-book/import | POST | 批量导入（词列表->生词本） |

### 4.2 AI 助记
| 接口 | 方法 | 功能 |
|------|------|------|
| /api/word/mnemonic | GET | 获取助记（优先缓存） |
| /api/word/mnemonic | POST | 生成助记并缓存 |

---

## 5. 页面修改

### 5.1 新增页面
- 批量导入页（`pages/import/import`）

### 5.2 现有页面调整
- 单词详情页增加“AI 助记”按钮与展示区
- “我的”页增加入口：
  - 批量导入/洗词

---

## 6. 开发计划

### 第一阶段
- 洗词接口与批量导入流程
- 前端批量导入页面

### 第二阶段
- AI 助记生成与缓存
- 详情页展示与刷新

---

## 7. 功能清单

### V3.0 新增
1. 批量导入与一键洗词
2. AI 语境辅助记忆（助记）

---

**文档版本**: V3.0  
**最后更新**: 2026-02-07
